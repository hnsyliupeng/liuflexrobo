# CHANGE_LOG

- v0.1 project scaffold, discovery (bpy 4.5.6 headless, Cycles CPU benchmark).
- v0.2 CHAR_001 model v1 + QC stills → arm length/rivet/tread-pitch/eye-flush fixes → model v2.
- v0.3 rig v1 (17 bones, drivers) → tread driver sign + modulo expression fixes.
- v0.4 forest asset families v1 → scatter v1 (697 objects) → material-slot repair (06).
- v0.5 firefly hero + GN swarm; layout v1 (10 cams); robot anim v1; firefly anim v1; lighting v1.
- v0.6 cycle-0 review: CRITICAL bone-parent bind fix (07), lighting v2 exposure, eye gain ×9,
  trunk/fern rebuild (04 v2), log move + cam re-place + clearance (05/08 v2), anim re-run (09/10).
- v0.7 preview 720-frame render + 30 s assembly (cycle 1) — pending.
- v0.8 cycle-1 fixes: ensure_slots self-heal; fern droop+narrower pinnae; ground-only scatter height;
  S1 camera pulled to 12 m; dusk sun/shaft rebalance; night fill/rim up; eye gain 4.0.
- v0.9 720-frame preview render (480x270 8spp OIDN) + editorial assembly — running.
- v1.0 cycle-2: night exposure uplift; arm reach numeric solve (09b); firefly scale/emission/practical-light
  rework; S9 macro cam reframe; firefly abdomen orientation at landing; night shots re-rendered.
